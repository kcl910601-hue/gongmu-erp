import { getLmeContext } from "@/lib/lme-server";
import { queryProjectCosts } from "@/lib/project-costs-server";
import type { ProjectCostSummary } from "@/lib/project-costs";
type Row=Record<string,unknown>&{cost_summary:ProjectCostSummary};
export async function GET(request:Request){const {supabase,employee}=await getLmeContext();if(!employee)return Response.json({error:"승인된 사용자만 조회할 수 있습니다."},{status:403});const params=new URL(request.url).searchParams;const clean=(value:string)=>value.replace(/[,%()]/g,"");let query=supabase.from("projects").select("id,project_code,project_name,client_name,site_address,salesperson,task_manager,status,process_type,start_date,end_date");const text=params.get("query")?.trim();if(text)query=query.or(`project_name.ilike.%${clean(text)}%,project_code.ilike.%${clean(text)}%`);for(const [column,key] of [["status","project_status"],["salesperson","salesperson"],["task_manager","task_manager"],["process_type","process_type"]] as const){const value=params.get(key);if(value)query=query.eq(column,value);}const from=params.get("start_date_from"),to=params.get("start_date_to");if(from)query=query.gte("start_date",from);if(to)query=query.lte("start_date",to);const result=await query.order("start_date",{ascending:false,nullsFirst:false}).limit(500);if(result.error)return Response.json({error:result.error.message},{status:500});const combined=await queryProjectCosts(supabase,result.data??[]);if(combined.error||!combined.data)return Response.json({error:combined.error?.message},{status:500});let rows=combined.data as Row[];const has=params.get("has_cost");if(has==="true"||has==="false")rows=rows.filter((row)=>row.cost_summary.has_cost_entries===(has==="true"));const category=params.get("category_code");if(category)rows=rows.filter((row)=>Boolean(row.cost_summary.category_breakdown[category]));const safe=rows.every((row)=>row.cost_summary.amounts_are_safe);const sum=(field:keyof ProjectCostSummary)=>rows.reduce((total,row)=>total+Number(row.cost_summary[field]??0),0);return Response.json({projects:rows,categories:combined.categories,kpi:{registered_project_count:rows.filter((row)=>row.cost_summary.has_cost_entries).length,unregistered_project_count:rows.filter((row)=>!row.cost_summary.has_cost_entries).length,total_supply_amount_krw:safe?sum("total_supply_amount_krw"):null,total_vat_amount_krw:safe?sum("total_vat_amount_krw"):null,total_amount_krw:safe?sum("total_amount_krw"):null,subcontract_supply_amount_krw:safe?sum("subcontract_supply_amount_krw"):null,transportation_supply_amount_krw:safe?sum("transportation_supply_amount_krw"):null,labor_supply_amount_krw:safe?sum("labor_supply_amount_krw"):null,installation_supply_amount_krw:safe?sum("installation_supply_amount_krw"):null,as_service_supply_amount_krw:safe?sum("as_service_supply_amount_krw"):null,other_supply_amount_krw:safe?sum("other_supply_amount_krw"):null,void_cost_count:sum("void_cost_count"),amounts_are_safe:safe}});}
